schema_version: 1

version: 1.0
name: jboss-amqmaas-1/amqmaas10-auth-openshift
description: "AMQMaas SSO for authentication"
from: redhat-sso-7/sso72:latest
labels:
    - name: "io.k8s.description"
      value: "AMQMaas Authentication using SSO"
    - name: "io.k8s.display-name"
      value: "AMQMaas Authentication"
    - name: "io.openshift.tags"
      value: "amqmaas, sso, authentication"
envs:
    - name: "VERSION"
      value: "latest"
    - name: "JAVA_OPTS"
      value: "-Dvertx.cacheDirBase=/tmp -Djboss.bind.address=0.0.0.0 -Djava.net.preferIPv4Stack=true"
packages:
    repositories:
      - jboss-os
      - jboss-ocp
      - jboss-epel
      - jboss-rhscl
      - jboss-optional
    install:
          - saxon
modules:
      install:
          - name: keycloak.install
          - name: keycloak.rhel.deploy
artifacts:
    - url: https://maas-jenkins.rhev-ci-vms.eng.rdu2.redhat.com/job/maas-test/lastSuccessfulBuild/artifact/keycloak/build/distributions/keycloak.tgz
      name: keycloak-bin.tar.gz
ports:
    - value: 9990       
        